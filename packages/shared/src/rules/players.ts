import type { PlayerColor } from '../types/game.js';

export const MIN_PLAYERS = 3;
export const MAX_PLAYERS = 6;

/** Assignment order for new players/bots joining a room. */
export const PLAYER_COLORS: PlayerColor[] = ['RED', 'BLUE', 'GREEN', 'YELLOW', 'ORANGE', 'PURPLE'];
