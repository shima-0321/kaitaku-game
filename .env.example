# Server port (Render sets this automatically in production)
PORT=3001

# development | production
NODE_ENV=development

# Port the Vite dev server proxies /socket.io to (matches server PORT during local dev)
SERVER_PORT=3001
